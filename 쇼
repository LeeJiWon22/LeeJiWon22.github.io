<?php
$n = 30;
$data = array();

// 랜덤한 정수 $n개 생성
for ($i = 0; $i < $n; $i++) {
    $data[$i] = rand(1, 100); // 1 이상 100 이하
}

// 원본 배열 출력
echo "생성된 랜덤 숫자들:<br>";
echo implode(", ", $data) . "<br><br>";

// 오름차순 정렬
sort($data);

// 정렬된 배열 출력
echo "정렬된 숫자들:<br>";
echo implode(", ", $data);
?>
