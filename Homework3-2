<?php
$n = 30;
$data = array();

for ($i = 0; $i < $n; $i++)
{
    $data[$i] = rand(1, 100);
}
echo "랜덤 숫자:<br>";
echo implode(", ", $data) . "<br><br>";

sort($data);

echo "정렬된 숫자:<br>";
echo implode(", ", $data);
?>
